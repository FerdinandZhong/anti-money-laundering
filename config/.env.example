# Copy to .env (never commit .env)
# Override config.yaml values per-deployment

LLM_PROVIDER=caii
CAII_ENDPOINT=https://<workspace-domain>/namespaces/serving-default/endpoints/<endpoint-name>
CDP_TOKEN=
VLLM_ENDPOINT=http://localhost:8000/v1
VLLM_API_KEY=
OLLAMA_ENDPOINT=http://localhost:11434/v1
BACKEND_PORT=7078
